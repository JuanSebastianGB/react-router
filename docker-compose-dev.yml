version: '3.8'
services:
  react-app:
    image: juansebastiangb/react-router-demo
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    volumes:
      - ./src:/app/src
